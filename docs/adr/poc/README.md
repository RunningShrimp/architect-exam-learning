# POC · 05 存储层次 3D 化可行性试点（2026-09-03）

> 本目录为 ADR-001 的评估证据存档。POC 运行环境位于临时构建目录（`/tmp/stack-test`），
> 依赖按 ADR-001 附录 A 构建后置于 `dist/`（three-esm.js 712.8KB / ogl-esm.js 129.6KB，
> gzip 187KB / 39KB——库模式全量导出口径，比真实 islands 摇树口径 134KB / 14.5KB 更保守）。

## 同屏对比项

- A：现有 2D 基线（SVG 塔形分层 + 访问光点 + 逐帧解说，零包增量）
- B：Three.js r185（动态 import，拖拽旋转 + 四控）
- C：OGL 1.0.11（动态 import，同上）

## 实测读数（CPU 4x 节流模拟低端机，桌面 Chrome）

| 项 | A 2D | B Three | C OGL |
|---|---|---|---|
| 包增量 | 0 | 187KB gz（保守口径） | 39KB gz（保守口径） |
| 加载+解析（缓存热） | — | 86ms | 22ms |
| 三舞台并发稳态帧率 | 121fps（合并采样，显示器满帧） | 同左 | 同左 |
| WebGL | — | WebGL2 ✅ | WebGL2 ✅ |
| 层名标签 | ✅ 每层 | ❌ 仅几何体 | ❌ 仅几何体 |
| 逐帧解说文字 | ✅ | ❌ | ❌ |

## 结论摘录（详见 ADR-001）

3D 版多出来的只有"可旋转的立体感"；考点信息（层次关系/命中率/访问路径）2D 已完整承载且文字更全。
**未找到"3D 比现有 2D 多教会了什么"的证据 → 触发学习价值一票否决 → NO-GO。**

## 复现步骤

1. 按 ADR-001 附录 A 构建 `dist/three-esm.js`、`dist/ogl-esm.js`；
2. 将本目录 `poc.html` 与 `dist/` 同置一目录，`python3 -m http.server 8112`；
3. 打开页面 → 点击两个「动态加载」按钮 → 读取各舞台 fps 与加载耗时；
4. 可选：DevTools Performance 面板 CPU 4x 节流复现帧率数据。
